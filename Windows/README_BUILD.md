# NeonDrive Windows Build

Target:

- Windows 64-bit
- Vulkan renderer
- EXE package
- Installer support


Build:

cmake -B build

cmake --build build --config Release


Installer:

Use NSIS with installer.nsi
