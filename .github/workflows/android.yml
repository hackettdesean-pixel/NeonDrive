name: Android Build

on:
  push:
    branches:
      - main

jobs:

  build-android:

    runs-on: ubuntu-latest

    steps:

    - uses: actions/checkout@v4

    - name: Android setup
      uses: android-actions/setup-android@v3

    - name: Build
      run: echo "Android build system ready"
